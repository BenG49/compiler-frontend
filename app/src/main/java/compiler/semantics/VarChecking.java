package compiler.semantics;

import java.util.ArrayList;
import java.util.List;

import compiler.parser.grammars.ast.AST;
import compiler.syntax.Type;

public class VarChecking {
    public static void check(AST program) {
        // recursively descent through the tree, assigns types and scopes to variables
        List<VarData> variables = new ArrayList<VarData>();

        
    }

}
